spring:
  datasource:
    url: jdbc:h2:./localdb;DB_CLOSE_ON_EXIT=FALSE;DB_CLOSE_DELAY=-1;
    driver-class-name: org.h2.Driver
    username: admin
    password: admin
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update